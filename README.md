# Delta Force Stats Site

## 실행
```bash
npm install
cp .env.local.example .env.local
npm run dev
```

## 환경변수
```env
DELTA_FORCE_API_URL=https://api.deltaforceapi.com
DELTA_FORCE_API_KEY=your_api_key_here
```

현재 버전은 UUID 기반 전적 조회만 지원합니다.
